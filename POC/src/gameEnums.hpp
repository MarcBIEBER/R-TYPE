enum spritesEnum {
    BACKGROUND,
    STARSHIP
};

enum buffBonus {
    BONUS_HEALTH,
    BONUS_DAMAGE,
    BONUS_SPEED
};

enum sceneEnum {
    MENU,
    GAME,
    END
};

enum clickEnum {
    PLAY,
    QUIT
};